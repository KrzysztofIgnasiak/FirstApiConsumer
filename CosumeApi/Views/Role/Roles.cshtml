@model IEnumerable<CosumeApi.Models.BindingModels.RoleViewBindingModel>

@{
    ViewBag.Title = "Roles";
}

<h2>Roles</h2>

<p>
    @Html.ActionLink("Create New", "CreateRole") |
    @Html.ActionLink("Add User To Role", "AddUserToRole") |
    @Html.ActionLink("Remove User From Role", "RemoveUserFromRole") |
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
        </td>
    </tr>
}

</table>
